// Retired sign-only entry point. The active page loads the simulation-only module.
// Keeping this import side-effect-free prevents old module URLs from invoking a wallet.
export {debugClick} from './pump-preview-debug.js';
